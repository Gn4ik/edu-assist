SUMMARY_PROMPT = """Ты ассистент для учебы. Составь краткий конспект по следующему тексту. Используй маркированный список из {max_points} пунктов. Пиши только на русском. Будь краток, выделяй только самое важное.

Текст: {text}

Конспект:"""

SUMMARY_TOPIC_PROMPT = """Ты ассистент для учебы. Составь краткий учебный конспект на тему "{topic}". Используй маркированный список из {max_points} пунктов. Пиши только на русском. Конспект должен быть информативным и структурированным.

Конспект:"""

FLASHCARD_PROMPT = """Создай {num_cards} карточек для запоминания по следующему тексту. Верни ТОЛЬКО JSON массив. Каждая карточка должна иметь поле "front" (вопрос или термин) и "back" (ответ или определение).

Пример формата:
[{{"front": "", "back": ""}}]


Текст: {text}

JSON ответ:"""

FLASHCARD_TOPIC_PROMPT = """Создай {num_cards} карточек для запоминания на тему "{topic}". Верни ТОЛЬКО JSON массив. Каждая карточка должна иметь поле "front" (вопрос или термин) и "back" (ответ или определение).

Пример формата:
[{{"front": "Что такое ...", "back": "Это ..."}}]

JSON ответ:"""

QUIZ_PROMPT = """Создай {num_questions} тестовых вопросов с 4 вариантами ответов по следующему тексту. Верни ТОЛЬКО JSON массив. Каждый вопрос должен иметь: "question" (вопрос), "options" (массив из 4 строк), "correct" (индекс правильного ответа от 0 до 3).

Пример формата:
[{{"question": "Вопрос?", "options": ["A", "B", "C", "D"], "correct": 0}}]

Текст: {text}

JSON ответ:"""

QUIZ_TOPIC_PROMPT = """Создай {num_questions} тестовых вопросов с 4 вариантами ответов на тему "{topic}". Верни ТОЛЬКО JSON массив. Каждый вопрос должен иметь: "question" (вопрос), "options" (массив из 4 строк), "correct" (индекс правильного ответа от 0 до 3).

Пример формата:
[{{"question": "Вопрос?", "options": ["A", "B", "C", "D"], "correct": 0}}]

JSON ответ:"""
